inittialState = []

const cartReducer = (state=inittialState, action) => {
    switch (action.type) {
        case value:
            
            break;
    
        default:
            break;
    }
}

export default cartReducer